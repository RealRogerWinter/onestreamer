# Trivy ignore file — deferred docker-build scan findings, WITH justification.
# Picked up automatically by `trivy image` (runs from the repo root in CI).
# Rule: every entry needs a rationale + a follow-up plan. Re-check on each bump.
#
# (currently empty — node-tar 6 -> 7 is now fixed directly via the root
#  package.json "tar": "7.5.11" override, so its CVEs no longer need ignoring.)
